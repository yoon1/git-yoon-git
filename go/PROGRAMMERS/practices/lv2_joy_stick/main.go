package main

func min(n ...int) (a int)  {
	a = n[0]
	for _, each := range n {
		if each < a {
			a = each
		}
	}
	return
}

func minToNext(cur, next rune) int {
	up := @
	down := @
	left := @
	right := @
}

func solution(name string) int {
	return 0
}
