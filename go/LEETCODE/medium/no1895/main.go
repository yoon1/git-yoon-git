package main

import (
	"fmt"
)

func largestMagicSquare(grid [][]int) int {
	var (
		mx = len(grid)
		nx = mx
	)
	fmt.Println(nx)

	return 1
}

type Square struct {
	grid [][]int
}

func (s *Square) rangeTotal() {

}
