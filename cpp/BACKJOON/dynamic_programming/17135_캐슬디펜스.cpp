#include <iostream>
#define MAX 15

using namespace std;

int N, M, D;
struct pos{
	int y,x;
};
int map[MAX][MAX] = { 0 };
int main() {
	// ют╥б
	cin >> N >> M >> D;
	for (int i = 0; i < N; i++) 
		for (int j = 0; j < M; j++) {
			scanf("%d", &map[i][j]);
			if ()
		}


	return 0;
}